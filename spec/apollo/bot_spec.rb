require 'spec_helper'

describe Apollo::Bot do
  it 'has a version number' do
    expect(Apollo::Bot::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
